<div class="container">
  <% if @expose %>
    image:<%= params[:image] %>, name:<%= params[:name] %>, port:<%= params[:port] %>で作成しました。<br>
    現在の状態は<%= link_to "こちら", status_show_path %>で確認してください
  <% else %>
    エラーが発生しました。<br>
    詳しくはログを確認してください
  <% end %>
</div>
